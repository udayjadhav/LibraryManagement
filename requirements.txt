# Backward compatibility for `pip install -r requirements.txt`.
# Preferred setup: pip install -e ".[dev]" (see README).
fastapi
uvicorn
sqlalchemy
pydantic
requests
email-validator
psycopg[binary]
pytest
httpx
